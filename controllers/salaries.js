let getSalariesByMin = require('./modules/salaries/getSalariesByMin');

// This file acts as a funnel to collect all of the modules and export them in one controller file for reference by the router.

module.exports = {
  getSalariesByMin
}